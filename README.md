For local development:

Make sure to start the `mysql` service using the following commands:
> sudo service mysql status
> sudo service mysql start --init-file=~/mysql-init
<!-- > sudo mysqld --init-file=<your-home-directory>/mysql-init & -->

Stop it with:
> sudo service mysql start --init-file=~/mysql-init