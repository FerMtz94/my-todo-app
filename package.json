{
  "name": "my-todo-app",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run --env-file=.env.development --watch index.ts",
    "local": "bun run --env-file=.env.production.local --watch index.ts",
    "start": "bun run --env-file=.env --watch index.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1",
    "mysql2": "^3.16.0"
  }
}